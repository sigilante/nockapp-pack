[package]
name = "solitaire"
version = "0.1.0"
description = "A playable Klondike Solitaire served over HTTP, with all game logic in the Hoon kernel."
authors = ["sigilante"]
license = "MIT"
template = "http-server"

[dependencies]
